<% tab 'offices' %>
<% title 'Offices' %>

<%= render partial: "shared/header/#{@ui_space}_header" %>

<div class="lp__scroll_containers">
  <div class="lp__fixed f12 no-gutters">
    <div class="row justify-content-center pt-3">
      <div class="lp__faux_modal narrow">
        <div class="row">

          <div class="col-md-12 pl-5 pr-5">
            <p class="text-center mt-5 pt-3">NEED ICON HERE</p>
            <p class="text-center pt-3 pl-4 pr-4"><strong><%= @office.name %> has been added to your list.</strong></p>

            <p class="text-center pt-4 pl-4 pr-4 pb-4">Now you can quickly schedule appointments, order food, and compile important information about this office.</p>

            <a href="" class="btn btn-sm btn-primary btn-block mt-4">Done</a>
            <a href="<%= calendar_rep_office_path(@office) %>" class="btn btn-sm btn-secondary btn-block">Book An Appointment</a>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>
